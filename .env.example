# Only used with docker-compose.server.yml.
CADDY_NETWORK=caddy

# Only used with docker-compose.local.yml.
# Keep 127.0.0.1 unless you explicitly want host-wide access.
STEUERRECHNER_BIND=127.0.0.1
STEUERRECHNER_PORT=8501
